# API application

NestJS Foundation API. It currently exposes only infrastructure routes:

- `GET /api/v1/health`
- `GET /api/v1/ready`

Product controllers and authentication flows are intentionally not implemented yet. See the root API contract and module-boundary documents before adding a feature.
