# Shared contracts

This package contains transport and infrastructure contracts only: API envelopes, error codes, pagination, IDs, system roles, identity interfaces, and versioned event conventions. Product domain models belong in their owning future modules.
